<div class="comment-avatar-wrapper">
    @if (Commenter is GuestCommenterResponse guestCommenter)
    {
        <img class="comment-avatar-img" src="https://cravatar.cn/avatar/@guestCommenter.EmailMd5" alt="@guestCommenter.NickName"/>
    }
    else if(Commenter is MemberCommenterResponse memberCommenter)
    {
        <img class="comment-avatar-img" src="@memberCommenter.Avatar" alt="@memberCommenter.NickName"/>
    }
</div>

@code {
    [Parameter]
    [EditorRequired]
    public required CommenterResponse Commenter { get; set; }
}
