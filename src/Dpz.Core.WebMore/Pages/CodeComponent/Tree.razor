<CascadingValue Value="this">
    <ul>
        @foreach (var item in Node.Directories)
        {
            <TreeNode OnSelectedFile="OnSelectedFile"
                      IsFolder="@(true)"
                      Name="@item.Name"
                      Path="item.CurrentPath"
                      OnExpandFolder="OnExpandFolder"
                      Keyword="@Keyword"/>
        }

        @foreach (var item in Node.Files)
        {
            <TreeNode OnSelectedFile="OnSelectedFile"
                      IsFolder="@(false)"
                      Name="@item.Name"
                      Path="item.CurrentPath"
                      OnExpandFolder="OnExpandFolder"
                      Keyword="@Keyword"/>
        }
    </ul>
</CascadingValue>